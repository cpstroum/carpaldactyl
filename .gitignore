__pycache__/
*.pyc

# Your real .env holds per-arm calibration AND secrets (e.g. the W&B key), so
# it must never be committed. Copy .env.example -> .env to get started.
.env
.env.local
wandb key.txt

# Scratch output from running demos (e.g. tools/probe_color.py captures).
outputs/
